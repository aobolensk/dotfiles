[include]
	path = .gitconfig_local
	# machine specific settings
[credential]
	helper = store
[alias]
	a = add
	b = branch
	c = commit
	cb = checkout -b
	co = checkout
	cp = cherry-pick
	d = diff
	ds = diff --staged
	l = log
	p = push
	pl = pull --prune
	rb = rebase
	s = status
	tree = log --graph --pretty=oneline --abbrev-commit
	undo = reset --hard
